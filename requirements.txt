# ─── Core dependencies ────────────────────────────────────────────────────────
ultralytics==8.4.23
streamlit>=1.43.0
Pillow>=10.0.0
opencv-python>=4.8.0

# ─── GPU acceleration (optional) ─────────────────────────────────────────────
#
# The packages above install CPU-only PyTorch by default.
# If you have a CUDA-compatible NVIDIA GPU, run the following AFTER the above:
#
#   pip install --pre torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/nightly/cu130
#
# Tested on: RTX 5050 Laptop · CUDA 13.0 · torch 2.12.0.dev+cu130
# For CUDA 11.8 / 12.1 replace cu130 with cu118 / cu121 accordingly.
